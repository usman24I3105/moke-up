[1mdiff --git a/app/api/generate-pdf/route.ts b/app/api/generate-pdf/route.ts[m
[1mindex 80f9941..929c02f 100644[m
[1m--- a/app/api/generate-pdf/route.ts[m
[1m+++ b/app/api/generate-pdf/route.ts[m
[36m@@ -12,13 +12,27 @@[m [mexport async function POST(request: NextRequest) {[m
 [m
     // Read the HTML template[m
     const templatePath = join(process.cwd(), "app", "templates", "itinerary.html");[m
[31m-    const template = readFileSync(templatePath, "utf-8");[m
[32m+[m[32m    let template: string;[m
[32m+[m[32m    try {[m
[32m+[m[32m      template = readFileSync(templatePath, "utf-8");[m
[32m+[m[32m    } catch (fileError) {[m
[32m+[m[32m      console.error("Template file error:", fileError);[m
[32m+[m[32m      console.error("Template path:", templatePath);[m
[32m+[m[32m      console.error("Current working directory:", process.cwd());[m
[32m+[m[32m      throw new Error(`Template file not found at ${templatePath}`);[m
[32m+[m[32m    }[m
 [m
     // Render the template with data[m
     const html = renderTemplate(template, data);[m
 [m
     // Determine if we're running locally or on Vercel[m
[31m-    const isLocal = process.env.NODE_ENV === "development";[m
[32m+[m[32m    const isVercel = process.env.VERCEL === "1" || process.env.VERCEL_ENV !== undefined;[m
[32m+[m[32m    const isLocal = !isVercel && process.env.NODE_ENV === "development";[m
[32m+[m
[32m+[m[32m    // Configure Chromium for Vercel[m
[32m+[m[32m    if (isVercel) {[m
[32m+[m[32m      chromium.setGraphicsMode(false);[m
[32m+[m[32m    }[m
 [m
     // Launch Puppeteer[m
     const browser = await puppeteer.launch({[m
[36m@@ -37,10 +51,15 @@[m [mexport async function POST(request: NextRequest) {[m
     const page = await browser.newPage();[m
 [m
     // Set content and generate PDF[m
[32m+[m[32m    // Use "load" instead of "networkidle0" for better Vercel compatibility[m
     await page.setContent(html, {[m
[31m-      waitUntil: "networkidle0",[m
[32m+[m[32m      waitUntil: "load",[m
[32m+[m[32m      timeout: 30000,[m
     });[m
 [m
[32m+[m[32m    // Wait a bit for any dynamic content to render[m
[32m+[m[32m    await new Promise(resolve => setTimeout(resolve, 1000));[m
[32m+[m
     const pdf = await page.pdf({[m
       format: "A4",[m
       printBackground: true,[m
[36m@@ -55,7 +74,7 @@[m [mexport async function POST(request: NextRequest) {[m
     await browser.close();[m
 [m
     // Return PDF as response[m
[31m-    return new NextResponse(new Uint8Array(pdf), {[m
[32m+[m[32m    return new NextResponse(pdf, {[m
       headers: {[m
         "Content-Type": "application/pdf",[m
         "Content-Disposition": `attachment; filename="travel-itinerary-${data.client_name.replace(/\s+/g, "-")}.pdf"`,[m
